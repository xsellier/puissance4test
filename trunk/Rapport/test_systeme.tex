
\section{Résultats}

\subsection{Tests de validation}

Nous allons voir ici les résultats des tests imaginés pour s'assurer que le programme obtenu respecte bien les termes précisés dans la spécification.\newline\newline


\subsubsection{Règles de puissance 4}

\begin{itemize}
\item \texttt{Test 1 : existence des configurations de jeu}\newline\newline
Partie Humain vs Humain : succès.\newline
Partie Humain vs Cpu niveau 1 : succès.\newline
Partie Humain vs Cpu niveau 2 : succès.\newline

\item \texttt{Test 2 : tailles de grilles}\newline

L'instanciation d'une grille 6x7 a été réalisée à de multiples reprises avec succès.\newline

Pour les autres dimensions, nous avons décidé de réaliser une analyse partitionnelle basée sur l'agrandissement et la réduction de la grille d'origine.\newline

Pour éprouver le programme nous avons défini une relation d'équivalence comme tel:\newline
Deux données de tests $dt1$ et $dt2$ (ici des entiers représentant les 2 dimensions de la grille) sont en relation : $dt1 \diamond dt2$ si et seulement si leur valeur appartient au même intervalle $I \in N$.\newline
On forme une partition de ces intervalles sur l'ensemble des valeurs logiques d'instantiation $ I_{logique}=[4,+\infty]$. On entend par valeur logique d'instanciation une valeur susceptible d'être instanciée car elle permet de jouer une partie (i.e on n'instanciera jamais une grille de dimensions -1,3 car une valeur négative n'a aucun sens et une valeur comprise entre 0 et 4 n'est pas suffisante pour qu'il soit possible de gagner). Voici donc les intervalles concernés :\newline

On suppose que $dtH$ représente la hauteur de la grille, $dtL$ la largeur. Les valeurs par défaut étant $dtH = 6$ et $dtL = 7$, on va s'interesser aux intervalles inférieurs et supérieurs et lancer les tests sur chaque combinaison des classes d'équivalences formées par la méthode expliquée ci-dessus. Ainsi, pour la hauteur, on obtient les intervalles $ I_{H1}=[4,6]$ , $I_{H2}=]6,+\infty]$ et pour la largeur, $I_{L1}=[4,7]$, $I_{H2}=]7,+\infty]$.

On détermine alors exhaustivement l'ensemble des combinaisons des classes d'équivalence :\texttt{$(I_{H1}$,$ I_{L1})$},\texttt{$(I_{H2}$,$ I_{L1})$},\texttt{$(I_{H1}$,$ I_{L2})$},\texttt{$(I_{H2}$,$ I_{L2})$}\newline

Enfin, on choisit arbitrairement une $dt$ par classe d'équivalence et on obtient les 4 jeux de test suivants dérivant des couples précédents:
 \texttt{(dtH = 4, dtL = 5)}, \texttt{(dtH = 15, dtL = 6)}, \texttt{(dtH = 5, dtL = 20)}, \texttt{(dtH = 9, dtL = 9)}.

Chacune de ces instantiations a été réalisée avec succès et le comportement du programme correspondait à nos atttentes.\newline \newline

\item \texttt{Test 3 : match nul}\newline
Nous avons joué une partie de manière à obtenir un match nul, un élément de l'interface graphique nous a effectivement averti de cette terminaison.\newline\newline
\end{itemize}

\begin{itemize}
\item \texttt{Test 4 : échantillon d'utilisateurs}\newline
L'ensemble des étudiants auquels nous avons soumis le programme ont été d'accord pour dire que les règles avaient été respectées lors de leur partie.\newline
\end{itemize}

\begin{itemize}
\item \texttt{Test 5 : parties aléatoires}\newline
Nous avons testé avec succès la progression et terminaison correcte de dizaines de parties générées à l'aide d'un algorithme aléatoire pour le choix du coup à jouer par les 2 joueurs ordinateurs. Cette fonction est accessible en démarrant le programme avec l'option \texttt{-test}.
\end{itemize}

\subsubsection{Configurations de jeu}

Il suffit d'un test fonctionnel basique dont l'objet est de vérifier que lorsque le programme est lancé, les différentes configurations de jeu sont disponibles.\newline\newline

\begin{itemize}
\item \texttt{Test 6 : accès aux différentes configurations}\newline
Chacun des boutons de l'interface graphique entraîne la création de la partie correspondante. Nous avons vérifié exhaustivement chaque bouton.\newline
\end{itemize}

\subsubsection{Types de joueurs}

\begin{itemize}
\item \texttt{Test 7 : deux niveaux de jeu}\newline
De multiples parties avec les 2 niveaux de jeu ont été réalisées avec succès.\newline
\end{itemize}
